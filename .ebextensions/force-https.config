# Forward HTTP connections to HTTPS.

files:
  "/etc/nginx/conf.d/elasticbeanstalk/force-https.conf":
    mode: "000755"
    owner: root
    group: root
    content: |
      if ($http_x_forwarded_proto != 'https') {
        return 301 https://$host$request_uri;
      }
