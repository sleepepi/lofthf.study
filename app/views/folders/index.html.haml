- @title = "Docs"

- content_for :header, @title
= render "internal/tabs"
- content_for :breadcrumbs do
  %ol
    %li.breadcrumb-muted docs
- content_for :sidebar, render("folders/sidebar")

#featured-docs=render "files/featured_docs", featured_documents: @featured_documents

#latest-uploads= render "files/latest_uploads", documents: @documents
