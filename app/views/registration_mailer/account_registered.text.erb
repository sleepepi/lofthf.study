Dear <%= @admin.username %>,

<%= @user.full_name %> <%= @user.email %> registered a new account on the <%= ENV["website_name"] %> website.

View the registration here:

<%= "#{ENV["website_url"]}/admin/users/#{@user.id}" %>
