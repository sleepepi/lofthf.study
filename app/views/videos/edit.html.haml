- @title = "Edit Video - #{@video.name_was}"

- content_for :header, @video.name_was
= render "internal/tabs"
- content_for :breadcrumbs do
  %ol
    %li= link_to "videos", videos_path
    %li= link_to @video.name_was.to_s.downcase, @video
    %li.breadcrumb-muted edit
- content_for :sidebar, render("videos/sidebar")

.dashboard-container
  = render "form", video: @video
